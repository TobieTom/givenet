# ══ Dependencies ═════════════════════════════════════════════
# Installed via `npm install` from package.json — never commit
node_modules/

# ══ Design reference files ═══════════════════════════════════
# Figma design mockups (157 PNGs) — development reference only
assets/design/
# Figma color scheme reference (16 PNGs) — tokens already extracted into CSS
assets/colors/

# ══ Documentation ════════════════════════════════════════════
# PDF project docs used during development, not needed in repo
docs/

# ══ Claude Code plans ════════════════════════════════════════
# AI-generated planning files — not part of the application
plans/

# ══ Environment & secrets ════════════════════════════════════
# Environment variable files (none exist yet, but prevent accidental commits)
.env
.env.local
.env.*.local

# ══ Firebase local tooling ═══════════════════════════════════
# Generated by Firebase CLI during local development/emulation
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
firestore-export/

# ══ OS-generated files ═══════════════════════════════════════
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
# macOS
.DS_Store
._*

# ══ Editor / IDE files ═══════════════════════════════════════
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.code-workspace

# ══ Logs & debug output ═════════════════════════════════════
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ══ Misc build / temp artifacts ══════════════════════════════
*.tmp
*.temp
*.swp
*~
